#include <stdio.h>
#include <stdlib.h>
#include "struct.h"

// Encodes the Huffman Tree representation using a pre-order traversal and write it to an ouput file
void preOrderHeader(TreeNode *tn, FILE *fptrOut);
